<%= form_with model: list, class: "auth-form" do |form| %>
  <% if list.errors.any? %>
    <div class="form-errors">
      <h2><%= pluralize(list.errors.count, "error") %> prohibited this list from being saved:</h2>
      <ul>
        <% list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-field">
    <label for="list_date">Shopping date</label>
    <%= form.date_field :date, id: "list_date" %>
  </div>

  <div class="form-actions">
    <%= form.submit "Create List", class: "button button-block button-large" %>
  </div>
<% end %>
